<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cbox.business.project.projectinfo.mapper.ProjectInfoMapper">

	<!-- 查询对象 -->
	<sql id="selectVO">
		<![CDATA[
			select 
				t.rec_id,
				t.project_id,
				t.info_menu_id,
				t.info_title,
				t.info_image,
				t.info_video,
				t.info_file,
				t.info_content,
				t.rec_status,
				t.rec_person,
				t.rec_time,
				t.rec_updateperson,
				t.rec_updatetime,
				u.nick_name as user_name
			from d_project_info t
			left join sys_user u on t.rec_person=u.user_name
		]]>
	</sql>

	<!-- listProjectInfo : 获取项目资料列表数据 -->
	<select id="listProjectInfo" parameterType="java.util.Map" resultType="java.util.HashMap">
		<include refid="selectVO"/>

		where t.rec_status='1'
		<if test="project_id != null and project_id != ''">
			 and t.project_id=#{project_id}
		</if>
		<if test="info_menu_id != null and info_menu_id != ''">
			 and t.info_menu_id=#{info_menu_id}
		</if>
		<if test="info_title != null and info_title != ''">
			 and t.info_title like concat('%',#{info_title},'%')
		</if>
		order by t.rec_updatetime desc
	</select>

	<!-- getProjectInfo : 获取指定id的项目资料数据 -->
	<select id="getProjectInfo" parameterType="java.util.Map" resultType="java.util.HashMap">
		<include refid="selectVO"/>

		where t.rec_status='1'
		and t.rec_id=#{rec_id}
	</select>

</mapper>