<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cbox.business.project.projectdefecttype.mapper.ProjectDefectTypeMapper">

	<!-- 查询对象 -->
	<sql id="selectVO">
		<![CDATA[
			select 
				t.rec_id,
				t.defect_group,
				t.defect_type_name,
				t.rec_status,
				t.rec_person,
				t.rec_time,
				t.rec_updateperson,
				t.rec_updatetime,
				u.nick_name as user_name
			from d_project_defect_type t
			left join sys_user u on t.rec_person=u.user_name
		]]>
	</sql>

	<!-- listProjectDefectType : 获取项目不利因素分类组列表数据 -->
	<select id="listProjectDefectType" parameterType="java.util.Map" resultType="java.util.HashMap">
		<include refid="selectVO"/>

		where t.rec_status='1'
		<if test="defect_group != null and defect_group != ''">
			 and t.defect_group=#{defect_group}
		</if>
		<if test="defect_type_name != null and defect_type_name != ''">
			 and t.defect_type_name like concat('%',#{defect_type_name},'%')
		</if>
	</select>

	<!-- getProjectDefectType : 获取指定id的项目不利因素分类组数据 -->
	<select id="getProjectDefectType" parameterType="java.util.Map" resultType="java.util.HashMap">
		<include refid="selectVO"/>

		where t.rec_status='1'
		and t.rec_id=#{rec_id}
	</select>

</mapper>