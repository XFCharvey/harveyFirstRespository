<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="com.cbox.business.project.argwbind.mapper.ArgwBindMapper">

	<!-- 查询对象 -->
	<sql id="selectVO">
		<![CDATA[
			select 
				t.rec_id,
				t.project_id,
				p.project_name,
				t.type,
				t.file_id,
				f.org_name,
				t.rec_status,
				t.rec_person,
				t.rec_time,
				t.rec_updateperson,
				t.rec_updatetime,
				u.nick_name as user_name
			from d_argw_bind t
			left join sys_user u on t.rec_person=u.user_name
			left join d_sys_file_record f on t.file_id=f.id
			left join d_project p on t.project_id=p.rec_id
		]]>
	</sql>

	<!-- listArgwBind : 获取阿融官微绑定列表数据 -->
	<select id="listArgwBind" parameterType="java.util.Map"
		resultType="java.util.HashMap">
		<include refid="selectVO" />

		where t.rec_status='1'
		<if test="project_id != null and project_id != ''">
			and t.project_id =#{project_id}
		</if>
		<if test="type != null and type != ''">
			and t.type =#{type}
		</if>
		<if test="file_id != null and file_id != ''">
			and t.file_id like concat('%',#{file_id},'%')
		</if>
	</select>

	<!-- getArgwBind : 获取指定id的阿融官微绑定数据 -->
	<select id="getArgwBind" parameterType="java.util.Map"
		resultType="java.util.HashMap">
		<include refid="selectVO" />

		where t.rec_status='1'
		and t.rec_id=#{rec_id}
	</select>

</mapper>